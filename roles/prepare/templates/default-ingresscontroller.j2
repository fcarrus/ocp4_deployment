apiVersion: operator.openshift.io/v1
kind: IngressController
metadata:
  name: default
  namespace: openshift-ingress-operator
spec:
  replicas: 1
  nodePlacement:
    nodeSelector:
      matchLabels:
        kubernetes.io/hostname: {{ groups.infra_nodes | first }}
